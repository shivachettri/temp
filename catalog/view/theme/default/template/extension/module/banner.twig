
<div class="textbnr hidden-xs">
    {% for banner in banners %}
    		<div class="beffect">
	        {% if banner.link %}
			    <a href="{{ banner.link }}">
			    <img src="{{ banner.image }}" alt="{{ banner.title }}" class="img-responsive" />
			    </a>
			    {% else %}
			    <img src="{{ banner.image }}" alt="{{ banner.title }}" class="img-responsive" />
		    {% endif %}
			</div>
			{# <div class="col-xs-6 textbdes">{{ banner.description }}<div><a href="{{ banner.link }}" class="btn btn-primary">shop now</a></div></div> #}
    {% endfor %}
</div>